---
date: 2026-02-20
tags:
  - type/note
  - status/completed
  - ai/agents
  - dev/python
  - project/yas
  - #daily
created: 2026-02-20
summary: OpenClaw 업데이트 + AI 서버 PC 노드 구축 + 안드로이드 폰 OpenClaw 노드 연결 완료
---

# 📓 AI 활용 일지 — 2026-02-20 (목)

> **오늘의 핵심**: 웬디의 하드웨어 생태계를 하루 만에 대폭 확장 — 서버 PC·안드로이드 폰·맥북 모두 OpenClaw 노드로 연결 완료

---

## 개요

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-02-20 (목) |
| 주요 성과 | AI 서버 노드 + 안드로이드 노드 + OpenClaw 업데이트 + 게이트웨이 버그 해결 |
| 사용 도구 | OpenClaw, ADB, Termux/proot, Tailscale, FastAPI, Python |
| 작업 인원 | 존 + 웬디(AI 어시스턴트) |

---

## 1. OpenClaw 업데이트 (08:40~09:10)

### 업데이트 내용
- 버전: `2026.2.6-3` → `2026.2.19-2`
- **Claude Sonnet 4.6 모델 추가** (`openclaw.json`에 `anthropic/claude-sonnet-4-6`)
- family 에이전트 삭제 (오류 유발 요소 정리)

### 맥북 노드 재연결
- LaunchAgent 서비스 방식으로 맥북에 `ai.openclaw.node` 설치 완료
- 존 요청: 간단한 대화는 맥북으로 말하면서 텍스트도 함께 보내기
- ⚠️ **미해결**: 서비스 모드에서 타임아웃 발생 — 토큰 누락 의심, 수동 `openclaw node run`은 정상 동작

---

## 2. AI 서버 PC 노드 연결 (10:00~11:15)

### 배경
- 집 서버 PC(GTX 1060 6GB)를 웬디의 AI 도구 실행 노드로 활용하기 위한 작업

### 연결 과정

| 단계 | 내용 | 결과 |
|------|------|------|
| Tailscale 설치 | 서버 PC에 Tailscale 설치 + 로그인 | ✅ |
| openclaw node install | 호스트가 로컬IP로 저장되는 문제 | `--force` 재설치로 해결 |
| 페어링 이슈 | pending API에 미표시 버그 | paired.json 수동 등록 |
| 게이트웨이 재시작 | 재시작 후 연결 | ✅ 성공 |

### AI 도구 설치 완료 현황

| 도구 | 기능 | 환경 | 상태 |
|------|------|------|------|
| faster-whisper | 음성 → 텍스트 (STT) | Python 3.13 | ✅ |
| rembg (GPU) | 배경 제거 | Python 3.13 | ✅ |
| ultralytics YOLOv11 | 객체 감지 | Python 3.13 | ✅ |
| PaddleOCR | 이미지 → 텍스트 | Python 3.13 | ✅ |
| Real-ESRGAN + gfpgan | 이미지 업스케일링 | Python 3.11 venv | ✅ |
| InsightFace + onnxruntime-gpu | 얼굴 분석 | Python 3.11 venv | ✅ |

### Python 3.11 venv 필요 이유
- `basicsr`, `insightface` 등 C 확장 빌드 필요 패키지는 **Python 3.13 비호환**
- 별도 venv (`C:\ai-tools\venv311`)로 분리

### basicsr 필수 패치
```python
# 수정 전
from torchvision.transforms.functional_tensor import rgb_to_grayscale
# 수정 후
from torchvision.transforms.functional import rgb_to_grayscale
```
파일 위치: `C:\ai-tools\venv311\Lib\site-packages\basicsr\data\degradations.py`

### FastAPI 서버 코드 생성

| 항목 | 내용 |
|------|------|
| 위치 | `C:\JohnCodeQ\ai-server-api\` |
| 파일 수 | 12개 (main.py + routers 6개 + workers 2개 + utils + requirements + README) |
| 포트 | 8100 |
| 엔드포인트 | `/api/stt`, `/api/remove-bg`, `/api/detect`, `/api/ocr`, `/api/upscale`, `/api/face-analyze` |

### 서버 연결 명령 (매번 서버 PC에서 실행)
```cmd
set OPENCLAW_GATEWAY_TOKEN=0d71260016702a84741d7ecdfe2fdb80a8018c51311f257f
openclaw node run --host wendy.tail9938b1.ts.net --port 443 --tls --display-name "AI-Server"
```

---

## 3. 안드로이드 OpenClaw 노드 설치 (12:20~18:35)

### 3-1. 분석 단계 (12:20~13:15)

- 관련 YouTube 영상 Gemini 서브에이전트로 분석
- **결론**: Termux 기본(Bionic)에서 `npm i -g openclaw`는 `koffi/spawn.h/renameat2` 호환 이슈로 실패 가능성 높음
- **해결책**: `proot-distro + Ubuntu` 우회 방식이 정답

### 3-2. ADB 연동 (13:00~13:15)
- `Google.PlatformTools 36.0.2` winget으로 설치
- `unauthorized` → 폰에서 USB 디버깅 RSA 허용 → `device` 상태로 전환

### 3-3. Ubuntu(proot) + OpenClaw 설치 (13:15~13:45)

```bash
# Termux에서
proot-distro install ubuntu
proot-distro login ubuntu

# Ubuntu 내부에서
apt update && apt upgrade -y
apt install -y curl git build-essential ca-certificates
# NodeSource 22.x 설치
curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
apt install -y nodejs
node -v   # v22.22.0
npm install -g openclaw@latest
openclaw --version  # 2026.2.19-2
```

### 3-4. 게이트웨이 pairing 버그 해결 (15:00~15:35) ⚠️ 핵심!

**문제**: 게이트웨이 재시작 시 `devices/paired.json`이 `{}`로 초기화되는 버그
- CLI, 맥북 노드 모두 `pairing required (1008)` 에러 발생

**해결 절차** ✅
1. `openclaw dashboard` 실행
2. 브라우저에서 `http://127.0.0.1:18789/#token=<토큰>` 접속
3. **Nodes 탭** → pending 요청 **Approve** 클릭
4. `openclaw nodes status` 확인

**결과**
- MacBook: paired · connected ✅
- AI-Server: paired · connected ✅
- CLI: 정상 연결 ✅

### 3-5. 안드로이드 노드 최종 연결 (15:35~18:00)

- `/sdcard/run_fg.sh` 스크립트 생성 → proot-distro login ubuntu → openclaw node run
- **Tailscale IP**: 100.77.239.13
- 상태: paired + connected ✅

### 3-6. Termux:API 브릿지 (18:00~18:35) ✅

- 40개 명령 사용 가능 (TTS, 카메라, SMS, WiFi 등)
- **TTS 방법** (다음에 구현):
```bash
proot-distro login ubuntu \
  --bind /data/data/com.termux/files/usr/bin/termux-tts-speak:/usr/local/bin/termux-tts-speak \
  -- bash -c "openclaw node run ..."
```

---

## 4. Project YAS 기획 정리

### 현재 상태
- v1, v2: Remotion(React 기반) 영상 렌더링 완료
- 서브에이전트가 `ffmpeg + ASS 자막` 방식으로 전환 → 존 판단: Remotion 방식으로 복귀 검토 중

### 아키텍처 확정
- 언어: TypeScript + Node.js (Remotion 생태계 통일)
- 데이터: `storyboard.json` = 중앙 데이터 계약
- Agent Teams 4명: Architect / ScriptDev / AssetDev / RenderDev
- MVP 목표: 토픽 → 쇼츠 1개 자동 생성

---

## 5. 옵시디언 인프라 기술서 작성

- 경로: `600 Specialties/웬디 하드웨어 생태계 - 인프라 기술서.md`
- Mermaid 다이어그램 포함
- 기기별 상세 스펙 + 시리/단축어/애플워치 섹션
- 실제 단축어 세팅 (URL, 에이전트, Tailscale 조건) 기록

---

## 6. 핵심 교훈 정리

| 교훈 | 상황 | 해결책 |
|------|------|--------|
| OpenClaw allowlist는 전체 경로 | basename만 쓰면 무시됨 | 반드시 절대 경로로 등록 |
| 게이트웨이 재시작 후 pairing 필요 | paired.json 초기화 버그 | openclaw dashboard → Nodes → Approve |
| Python 3.13 호환성 | C 확장 패키지 빌드 실패 | 3.11 venv 별도 생성 |
| Termux 직접 설치 실패 | koffi/renameat2 이슈 | proot Ubuntu 우회 |

---

## 7. 남은 할 일

- [ ] AI Server FastAPI 6개 엔드포인트 실제 테스트
- [ ] OpenClaw allowlist에 git 전체 경로 등록
- [ ] Project YAS Remotion 자막싱크 수정
- [ ] 맥북 TTS 서비스 모드 타임아웃 수정
- [ ] 안드로이드 TTS 바인드마운트 적용

---

*작성: 웬디 (AI 어시스턴트) | 검토: 존*
